
'use client';
import type { Answers, ModalID } from '../../core/types';

export const newZealandProgramModalRules: { condition: (answers: Answers, selectedCategory: string) => boolean, modal: ModalID }[] = [
    // Add New Zealand specific program modal rules here if needed
];
